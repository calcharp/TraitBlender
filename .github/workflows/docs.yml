name: Deploy Documentation

on:
  push:
    branches:
      - v2  # Your current branch (change to 'main' or 'master' if you merge to default branch)
    paths:
      - 'TraitBlender/docs/**'
      - 'TraitBlender/mkdocs.yml'
      - '.github/workflows/docs.yml'
  workflow_dispatch:  # Allows manual triggering

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs-material
          pip install pymdown-extensions

      - name: Find addon directory
        id: addon_dir
        run: |
          ADDON_DIR="$(find . -maxdepth 1 -type d -iname 'traitblender' | head -1)"
          if [ -z "$ADDON_DIR" ]; then echo "Addon directory not found"; exit 1; fi
          echo "dir=$ADDON_DIR" >> $GITHUB_OUTPUT

      - name: Build documentation
        working-directory: ${{ steps.addon_dir.outputs.dir }}
        run: mkdocs build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ steps.addon_dir.outputs.dir }}/site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

        

