

Get Blender + TraitBlender into a Docker container that will work for V2. Maybe have a GitHub action that will build it automatically.

- Ask Imageomics people about getting TB on GitHub, and also some simulated datasets on HuggingFace.

- Make sure the dataset switches when the morphospace switches. 

DO NOT REMOVE:
cls; rm -rf traitblender-2.0.0.zip; blender --command extension build --source-dir TraitBlender/